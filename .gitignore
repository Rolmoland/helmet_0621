# ==================== MDK-ARM 编译产物 ====================
# 编译输出目录（.o .d .crf .axf .hex .bin .map .lst 等）
MDK-ARM/*/
MDK-ARM/*.lst

# Keil 用户私有配置（包含本机路径，不应共享）
MDK-ARM/*.uvguix.*
MDK-ARM/*.uvoptx

# ==================== STM32CubeMX ====================
# CubeMX 项目文件保留（.ioc .mxproject），但忽略生成的临时文件
# （如有需要可取消注释下面两行以忽略 CubeMX 生成的全部代码）
# Core/
# Drivers/

# ==================== VS Code ====================
# 编译器路径、调试配置等包含本机路径，按需取舍
# 如果团队统一工具链则可保留，否则忽略
# .vscode/

# ==================== 操作系统 ====================
.DS_Store
Thumbs.db
desktop.ini

# ==================== Claude Code ====================
.claude/
